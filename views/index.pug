extends layout

block content
  h1= title
  p Welcome to Kandimalla Assignment 10
  button(type='button',onclick='update()',class='btn btn-primary') press here
  
  div(id="target") text of Display 

  script.
    var client_count = 0;
    function update(){
      client_count +=1;
      document.getElementById("target").innerHTML = "Value now " + client_count;
    }