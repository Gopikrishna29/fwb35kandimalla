extends layout

block content
  h1= title
  - let image_names = ['jet1.jpg','jet2.jpg','jet3.jpg','jet4.jpg','jet5.jpg']
  - for(let i=0;i<3;i++){
      - let rno = Math.floor(Math.random()*5)
      - let iname = image_names[rno]

      img(src=`images/${iname}` alt='jet' style='width:300px; height:300px;')
      |    
  - }